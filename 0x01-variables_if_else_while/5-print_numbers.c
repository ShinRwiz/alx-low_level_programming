#include <stdio.h>

/**
 * main - Entry point
 * Description: 5-
 * Return: Always O (Success)
 */
int main(void)
{
	int num;

	for (num = 0; n < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
