#include <stdio.h>

/**
 * main - Entry point
 * Description: 6-
 * Return: Always O (Success)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		putchar(num);
	putchar('\n');
	return (0);
}
