#include <stdio.h>
#include <stdlib.h>
/**
 * main --
 * @argc: --
 * @argv: --
 * Return: --
 */
int main(int argc, char *argv[])
{
	int a;

	if (argc != 2)
	{
		printf("Error\n"), exit(1);
	}

	a = atoi(argv[1]);

	if (bytes < 0)
	{
		printf("Error\n"), exit(2);
	}

	return (0);
}
